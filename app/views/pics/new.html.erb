<div class="steamy-hottub">
	<h2> 
		yoyoyo


		<%= simple_form_for @pic do |f| %>
			<%=  f.input :hour %>
			<%= f.input :emotion %>
			<%= f.input :learned %>

			<%= f.submit 'Save' %>
		<% end %>

	</h2>
</div>